eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8761/eureka/
  instance:
    preferIpAddress: true
server:
  port: 12800
spring:
  application:
    name: govern-config
  cloud:
    config:
      label: develop
      server:
        git:
          searchPaths: daijie-example-govern/daijie-example-govern-config/config
          uri: https://github.com/daijiejay/daijie-example.git
